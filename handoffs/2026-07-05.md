# Session Handoff — 2026-07-05

**Project:** KnowFlow (vibe coding/four)
**Branch:** `main`
**Summary:** KnowFlow MVP fully implemented — all 25 tasks complete, build passes, tests pass.

## Work Completed

### Design Phase (Previous Session)
- Created full design specification: `docs/superpowers/specs/2026-07-05-knowflow-design.md`
- Created implementation plan: `docs/superpowers/plans/2026-07-05-knowflow-mvp.md` (25 tasks, 8 phases)

### Implementation Phase (This Session)
All 25 tasks completed:

| Phase | Tasks | Status |
|-------|-------|--------|
| 1. Foundation | Task 1-2: Next.js init, deps | ✅ |
| 2. Data Layer | Task 3-6.5: Types, Dexie, Supabase, Zustand stores | ✅ |
| 3. UI Components | Task 7-12: Button/Input/Modal, Layout, Home, Capture, Entry Detail, Cards | ✅ |
| 4. AI Integration | Task 13-15: Local mode, API mode, Processing pipeline | ✅ |
| 5. Search & Graph | Task 16-18: Search, Knowledge Graph, Settings | ✅ |
| 6. Export & PWA | Task 19-20: Export/Import, PWA manifest/SW | ✅ |
| 7. Sync | Task 21-22: Sync Manager, ClientLayout integration | ✅ |
| 8. Edge Functions & Polish | Task 23-25: Edge Functions, Responsive CSS, README | ✅ |

### Key Fixes Applied
- Supabase client: lazy initialization (build fails without env vars)
- Jest config: `setupFilesAfterSetup` → `setupFilesAfterEnv` (Jest 30)
- d3-force types: cast callback parameter for `forceLink().id()`
- Edge Functions: excluded from tsconfig (Deno imports)
- `@types/jest`: installed for test type support
- Supabase `.env.local`: placeholder created

## Key Decisions
- **Version choices:** Next.js 16 / React 19 / Tailwind 4 (latest, not spec's older versions)
- **Graph library:** d3-force + raw SVG
- **Auth:** Supabase Auth with anonymous fallback
- **Conflict resolution:** Last-write-wins
- **Layout:** Server Component + ClientLayout wrapper
- **AI fallback:** Generic `withFallback<TApi, TLocal>()` for type-safe API/local switching

## Files Changed (42 files)
- `knowflow/src/types/` — 7 type definition files (entry, node, link, draft, settings, conflict, index)
- `knowflow/src/lib/db/` — dexie.ts, supabase.ts (lazy init)
- `knowflow/src/stores/` — entryStore, nodeStore, uiStore, draftStore
- `knowflow/src/components/ui/` — Button, Input, Modal
- `knowflow/src/components/layout/` — Header, Navigation, FAB, ClientLayout, CaptureModal
- `knowflow/src/components/entry/` — EntryCard, EntryDetail
- `knowflow/src/components/cards/` — Card1Input, Card2Categorize, Card3Angles, Card4Extract
- `knowflow/src/components/search/` — SearchModal
- `knowflow/src/components/graph/` — KnowledgeGraph
- `knowflow/src/lib/ai/` — local.ts, api.ts, fallback.ts, process.ts
- `knowflow/src/lib/utils/` — language.ts, keywords.ts, search.ts, export.ts, import.ts
- `knowflow/src/lib/sync/` — syncManager.ts, conflictResolver.ts
- `knowflow/src/app/` — page.tsx, layout.tsx, login/page.tsx, graph/page.tsx, settings/page.tsx, library/[id]/page.tsx
- `knowflow/public/` — manifest.json, sw.js, icon.svg
- `knowflow/supabase/` — migrations/001_create_tables.sql, 3 Edge Functions
- `knowflow/tests/unit/` — dexie.test.ts
- `knowflow/README.md` — updated
- `knowflow/jest.config.js` — fixed setupFilesAfterEnv
- `knowflow/tsconfig.json` — excluded supabase/

## Current State
- **Builds:** ✅ `npm run build` succeeds (all 7 routes)
- **Type-check:** ✅ `npx tsc --noEmit` passes
- **Tests:** ✅ 6 tests pass (Dexie DB schema)
- **Routes:** `/`, `/login`, `/graph`, `/settings`, `/library/[id]`
- **Deployed:** No

## Blockers / Open Questions
- Supabase project not yet created — user needs to create at supabase.com and set `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` in `.env.local`
- Edge Functions are stubs (mock responses) — need real AI integration
- Node type "keyword" vs "concept" boundary — deferred to post-MVP
- Interval repetition algorithm — deferred to post-MVP

## Next Steps
1. User creates Supabase project and configures env vars
2. Run SQL migration (`supabase/migrations/001_create_tables.sql`) in Supabase dashboard
3. Test auth flow end-to-end
4. Implement real AI integration in Edge Functions (replace stubs)
5. Add more tests
6. Deploy to Vercel

## Context for Next Session
- **Working directory:** `c:\Users\zhong\Desktop\vibe coding\four\knowflow`
- **Repo root:** `c:\Users\zhong\Desktop\vibe coding\four`
- **Progress ledger:** `.superpowers/sdd/progress.md` — all tasks marked complete
- **Plan file:** `docs/superpowers/plans/2026-07-05-knowflow-mvp.md`
- **To verify:** `npm run build && npm test` from knowflow/

---

### Session 2 — 2026-07-05 (Supabase Setup + i18n)

## Work Completed

### Supabase Configuration
- Configured `.env.local` with real Supabase credentials (project: `tnurpklhndcwfmrwkdns`)
- Verified database connection — entries table accessible, count returns 0
- SQL migration tables already existed from prior setup; RLS policies also already in place
- Updated migration SQL to be idempotent (`CREATE TABLE IF NOT EXISTS`, `DROP POLICY IF EXISTS`)

### Bug Fixes
- **Dexie `.equals(undefined)` crash:** All 3 files using `db.entries.where('deletedAt').equals(undefined)` failed at runtime. Changed to `db.entries.filter(e => !e.deletedAt)` in:
  - `src/stores/entryStore.ts`
  - `src/stores/nodeStore.ts`
  - `src/lib/utils/search.ts`

### i18n System (Chinese/English Toggle)
- Created `src/lib/i18n/locales/en.ts` — 96 translation keys (English)
- Created `src/lib/i18n/locales/zh.ts` — 96 translation keys (Chinese)
- Created `src/lib/i18n/index.ts` — `useI18n()` hook returning `t('key')` function
- Updated `src/stores/uiStore.ts`:
  - Added `language: 'en' | 'zh'` state
  - Added `setLanguage()` action with localStorage persistence
  - Added `initLanguage()` for post-hydration language loading
- Updated all user-facing components with `t()` calls:
  - `src/app/page.tsx` — home page
  - `src/app/settings/page.tsx` — settings (added language toggle section)
  - `src/app/login/page.tsx` — login page
  - `src/components/layout/Navigation.tsx` — nav labels
  - `src/components/layout/CaptureModal.tsx` — capture flow
  - `src/components/layout/ClientLayout.tsx` — calls `initLanguage()` on mount
  - `src/components/search/SearchModal.tsx` — search UI
  - `src/components/graph/KnowledgeGraph.tsx` — empty state
  - `src/components/entry/EntryDetail.tsx` — card titles, status text

### Hydration Fix
- Initial implementation caused SSR/client mismatch (server=English, client=Chinese from browser detection)
- Fixed by: always start with `'en'` in store, load saved language in `useEffect` via `initLanguage()` after hydration

## Key Decisions
- **i18n approach:** Custom `useI18n()` hook with flat key-value dict — no external library, simple for MVP
- **Language detection:** Auto-detect from `navigator.language` on first visit, then persist to localStorage
- **Hydration strategy:** Default `'en'` on both server/client, update client-side after mount

## Current State
- **Builds:** ✅
- **Type-check:** ✅
- **Tests:** ✅ 6 pass
- **Supabase:** ✅ Connected (project `tnurpklhndcwfmrwkdns`)
- **Dev server:** Runs on http://localhost:3000
- **Known issue:** Windows Firewall blocks LAN access to port 3000 (localhost works fine)

## Next Steps
1. Test auth flow end-to-end (enable Email auth in Supabase dashboard)
2. Implement real AI integration in Edge Functions (replace stubs)
3. Add more tests (stores, i18n, AI processing)
4. Deploy to Vercel

---

### Session 3 — 2026-07-05 (UI Polish + Supabase Lazy Init + Refinements)

## Work Completed

### Supabase Client Refactor
- `src/lib/db/supabase.ts` — Replaced eager `createClient()` with lazy `getSupabase()` function
  - Build no longer fails when env vars are missing (returns dummy client with warning)
  - Added backward-compat Proxy export so existing `supabase.method()` calls still work
  - Marked old `supabase` export as `@deprecated`

### Home Page Rewrite
- `src/app/page.tsx` — Converted from static placeholder to functional client component
  - Loads entries from `entryStore` on mount
  - Displays entries in responsive grid (1/2/3 columns)
  - Empty state with emoji + i18n text
  - Loading state spinner

### UI Component Polish
- `src/components/ui/Button.tsx` — Added proper variant styles (primary/secondary/ghost), size styles (sm/md/lg), focus ring, hover transitions
- `src/components/ui/Input.tsx` — Added focus ring, error border state, better formatting

### Layout & PWA Metadata
- `src/app/layout.tsx` — Switched from Geist to Inter font, added PWA metadata (manifest, themeColor, viewport), wrapped children in ClientLayout
- `src/app/globals.css` — Custom scrollbar styles, mobile/desktop responsive utility classes

### i18n Integration
- `src/app/login/page.tsx` — Added `useI18n()` calls for signIn and continueAnon text

### Build Config
- `knowflow/tsconfig.json` — Added `supabase` to exclude (Edge Functions use Deno imports)
- `knowflow/package.json` — Added `test` and `test:watch` scripts with `--passWithNoTests`

### Documentation
- `CLAUDE.md` — Added full KnowFlow developer log section (project overview, tech stack, design decisions, data model, routes)

## Key Decisions
- **Supabase lazy init:** Prevents build failures when env vars aren't set — returns a dummy client with console warning instead of crashing
- **Inter over Geist:** Simpler font setup, Inter is widely used and well-supported
- **Button/Input polish before features:** Get the foundational UI components right before building more screens

## Current State
- **Builds:** ✅ `npm run build` succeeds (6 routes: /, /_not-found, /graph, /library/[id], /login, /settings)
- **Type-check:** ✅
- **Tests:** ✅ 6 pass
- **Supabase:** ✅ Connected (project `tnurpklhndcwfmrwkdns`)
- **All changes:** Uncommitted (12 modified files + many new files)

## Files Changed This Session
- `knowflow/src/lib/db/supabase.ts` — lazy init + Proxy backward compat
- `knowflow/src/app/page.tsx` — client component with entry grid
- `knowflow/src/app/layout.tsx` — Inter font, PWA metadata, ClientLayout wrapper
- `knowflow/src/app/login/page.tsx` — i18n calls
- `knowflow/src/app/globals.css` — scrollbar + responsive utilities
- `knowflow/src/components/ui/Button.tsx` — variant/size/focus styles
- `knowflow/src/components/ui/Input.tsx` — focus/error styles
- `knowflow/tsconfig.json` — exclude supabase/
- `knowflow/package.json` — test scripts
- `CLAUDE.md` — KnowFlow developer log

## Next Steps
1. Commit these changes (UI polish + Supabase lazy init)
2. Test auth flow end-to-end (enable Email auth in Supabase dashboard)
3. Implement real AI integration in Edge Functions (replace stubs)
4. Add more tests (stores, i18n, AI processing)
5. Deploy to Vercel

## Context for Next Session
- **All changes are uncommitted** — run `git add` and `git commit` before starting new work
- **Supabase env vars:** Already configured in `.env.local` (project `tnurpklhndcwfmrwkdns`)
- **Font:** Using Inter (not Geist) — set in layout.tsx
- **Viewport metadata warning:** Next.js warns about viewport in metadata export — should move to separate `generateViewport()` export if it bothers you

---

### Session 4 — 2026-07-05 (Tests + Commit + Deploy)

## Work Completed

### Test Suite (104 tests)
Created 9 new test files covering all core modules:
- `tests/unit/language.test.ts` — 10 tests for `detectLanguage()` (zh/ja/ko/en/mixed/empty)
- `tests/unit/keywords.test.ts` — 9 tests for `extractKeywords()` (stop words, frequency, count)
- `tests/unit/uiStore.test.ts` — 11 tests (toggle, language persistence, initLanguage)
- `tests/unit/ai-local.test.ts` — 14 tests (processCard1-4, processLocally)
- `tests/unit/ai-fallback.test.ts` — 8 tests (withFallback state machine, degraded recovery)
- `tests/unit/entryStore.test.ts` — 12 tests (CRUD, soft delete, syncStatus)
- `tests/unit/draftStore.test.ts` — 11 tests (30-day expiry, convert, hard delete)
- `tests/unit/search.test.ts` — 11 tests (scoring weights, soft-delete filter, cap at 20)
- `tests/unit/import.test.ts` — 7 tests (JSON import, dedup, error handling)

### Test Infrastructure
- Installed `fake-indexeddb` for IndexedDB polyfill in jsdom
- `jest.setup.js` — Added `fake-indexeddb/auto` import + `structuredClone` polyfill

### Git + Deploy
- Committed all MVP work: `03d0288` (78 files, +16809 lines)
- Changed git remote from `obra/superpowers.git` to `dcctc5kf7z-byte/KnowFlow.git`
- Pushed to https://github.com/dcctc5kf7z-byte/KnowFlow.git
- Installed Vercel CLI, authenticated, deployed to production
- Set Supabase env vars on Vercel (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
- Production URL: https://knowflow-blond.vercel.app (unreachable from China — network issue)

## Key Decisions
- **fake-indexeddb over mocking:** Real Dexie tests with in-memory IndexedDB are more reliable than mock-based tests
- **structuredClone polyfill:** jsdom doesn't provide it; JSON.parse(JSON.stringify()) is sufficient for test data
- **New git remote:** User's own repo at `dcctc5kf7z-byte/KnowFlow.git` instead of upstream superpowers

## Current State
- **Builds:** ✅
- **Tests:** ✅ 104 pass
- **Vercel:** ✅ Deployed (but unreachable from China)
- **Local:** ✅ Works on http://localhost:3000

---

### Session 5 — 2026-07-05 (UI Fixes — Modal, Processing, Delete, Graph)

## Work Completed

### Modal Overlay Fix
- `src/components/ui/Modal.tsx` — Added `z-10` to content panel (was behind backdrop), changed `bg-black bg-opacity-50` to `bg-black/50` (Tailwind v4 syntax), added Escape key handler

### Card Processing Flow
- `src/components/layout/CaptureModal.tsx` — After `createEntry()`, now calls `processEntry()` automatically, then navigates to `/library/{id}`. Added loading state on "Start" button.
- `src/components/entry/EntryDetail.tsx` — Added "Process Cards" button (visible when cards are pending/failed), calls `processEntry()` and refreshes

### Delete Functionality
- `src/components/entry/EntryCard.tsx` — Hover-visible ✕ button with `confirm()` dialog, calls `deleteEntry()`
- `src/components/entry/EntryDetail.tsx` — Delete button with inline confirmation UI, navigates to `/` after deletion

### Knowledge Graph Fix
- `src/components/graph/KnowledgeGraph.tsx` — Fixed empty state check (was `entries.length`, now `nodes.length`), deduplicated links using Set

### Navigation + i18n
- `src/app/library/[id]/page.tsx` — Added back button (← 返回), i18n for loading/notFound states, passes `onUpdate` to EntryDetail
- `src/lib/i18n/locales/en.ts` + `zh.ts` — Added keys: `detail.process`, `detail.delete`, `detail.deleteConfirm`, `detail.cancel`, `detail.confirmDelete`, `detail.loading`, `detail.notFound`, `detail.back`, `capture.processing`

### Null Safety
- `src/components/entry/EntryDetail.tsx` — Safe defaults for `entry.angles`, `entry.goldenQuotes`, `entry.tags`, `entry.cardStatus`
- `src/components/entry/EntryCard.tsx` — Safe defaults for `entry.cardStatus`, `entry.tags`

## Key Decisions
- **Auto-process on capture:** User expects immediate results after input, not a separate "process" step
- **Delete with confirmation:** Both card-level (confirm dialog) and detail-page (inline UI) patterns
- **Null-safe entry fields:** Entries from DB may have been created before all fields were required

## Commits This Session
- `0840f09` — fix(knowflow): UI fixes — modal overlay, card processing, delete, graph
- `583de8d` — fix(knowflow): null safety for EntryDetail and EntryCard

## Current State
- **Builds:** ✅
- **Tests:** ✅ 104 pass
- **Local:** ✅ Works on http://localhost:3000
- **Vercel:** Deployed but unreachable from China
- **Working tree:** Clean

## Next Steps
1. Test full flow: capture → process → view cards → delete → verify graph updates
2. Implement real AI integration in Edge Functions (replace stubs with OpenAI/Claude)
3. Vercel access issue — consider custom domain or alternative hosting for China
4. Add more test coverage for new UI features (EntryDetail actions, CaptureModal processing)

## Context for Next Session
- **Dev server:** Run `cd knowflow && npm run dev` — localhost:3000
- **Supabase project:** `tnurpklhndcwfmrwkdns` — env vars in `.env.local` and Vercel
- **Remote:** `origin` → `https://github.com/dcctc5kf7z-byte/KnowFlow.git`
- **Processing scenarios:** `quick_capture` only runs card1; `deep_digest`/`writing_material`/`knowledge_link` run all 4 cards and create graph nodes
- **Known issue:** Next.js viewport metadata warnings — cosmetic only, move to `generateViewport()` to fix

---

### Session 6 — 2026-07-05 (Bidirectional Links, Graph Redesign, Null Safety)

## Work Completed

### Bidirectional Links (Wiki-Links)
- Created `src/lib/utils/wikiLinks.ts` — full wiki-link system:
  - `extractWikiLinks()` — parses `[[Entry Title]]` and `[[Entry Title|alias]]` syntax
  - `resolveWikiLinks()` — matches link targets to actual entries (exact + partial match)
  - `buildBacklinks()` — finds all entries referencing a given entry
  - `wikiLinksToMarkdown()` — converts `[[links]]` to markdown `[links](/library/id)`
  - `generateMarkdown()` — full markdown output per entry (title, metadata, summary, content, quotes, angles, keywords)
- Added `linkedEntryIds: string[]` and `markdownContent: string` to `Entry` type
- Added `Backlink` interface: `{ entryId, entryTitle, context, offset }`
- Updated `src/lib/ai/process.ts` to extract wiki-links during processing and create bidirectional connections
- Updated Dexie schema to v2 with migration for new fields

### Knowledge Extraction Overhaul (`src/lib/ai/local.ts`)
- **Card 1 — Title:** First meaningful sentence instead of 50-char truncation; entity extraction (proper nouns, parenthesized terms, tech patterns)
- **Card 2 — Categorization:** Rule-based scoring across 9 categories (Tech/Sci/Phil/Biz/Health/Edu/Creative/Life/Other) with Chinese + English signal words; sub-category detection (Tech→Frontend/Backend/DevOps/AI, Phil→Epistemology/Ethics/Metaphysics)
- **Card 3 — Angles:** Category-specific angle pools (5 per category); content-aware angles (problems, comparisons, opinions); generic learning angles; 3-5 angles per entry
- **Card 4 — Quotes:** Multi-strategy extraction (highlighted text, quoted text, signal-word sentences, definition patterns, substantial sentences); deduplication

### Entry Detail — Clickable Cards + Markdown (`src/components/entry/EntryDetail.tsx`)
- Step bar is now clickable buttons that expand/collapse cards and scroll to them
- Accordion-style card sections with status color chips
- Markdown rendering via `react-markdown` + `remark-gfm` for card content
- Backlinks panel showing entries that reference the current one
- Linked entries panel showing forward connections
- Markdown source toggle at bottom
- Safe defaults for all entry fields (fixed crash with old DB entries)

### Knowledge Graph Redesign (`src/components/graph/KnowledgeGraph.tsx`)
- Cluster-based coloring by node type (keyword=blue, concept=purple, person=green, tool=amber)
- Interactive hover — highlights connected nodes, dims unrelated ones
- Click node → navigate to entry
- Zoom controls (in/out/reset)
- Legend showing node type colors
- Hover tooltip with node details
- Grid dot background for depth
- Glow effect on active nodes

### Entry Card Improvements (`src/components/entry/EntryCard.tsx`)
- Progress bar showing processing completion percentage
- Keywords preview (first 3 in amber chips)
- Linked count indicator (🔗)
- Better structured layout: title → summary → keywords → tags → meta

### Home Page Grouping (`src/app/page.tsx`)
- Smart time-based grouping: 本周新捕获 (green) / 本月 (blue) / 更早 (gray)
- Stats bar showing entry count and linked count
- Max-width container for readability

### Navigation & UI Polish
- `Navigation.tsx` — backdrop blur, icons, centered layout
- `FAB.tsx` — moved up to `bottom-24` to avoid overlapping bottom nav
- `globals.css` — full prose/markdown styling (headings, blockquotes, code, tables, links, lists)

### Dependencies Added
- `react-markdown` — markdown renderer
- `remark-gfm` — GitHub Flavored Markdown
- `remark-wiki-link` — wiki-link syntax support
- `rehype-raw` — HTML in markdown

### Null Safety Fixes
Fixed `Cannot read properties of undefined (reading 'length')` crash for old DB entries missing new fields:
- `EntryCard.tsx` — `keywords`, `linkedEntryIds`
- `EntryDetail.tsx` — `keywords`, `extractedNodes`
- `Card2Categorize.tsx` — `tags`, `keywords`
- `Card3Angles.tsx` — `angles`
- `Card4Extract.tsx` — `goldenQuotes`, `extractedNodes`
- `export.ts` — `goldenQuotes`, `tags`
- `wikiLinks.ts` — `goldenQuotes`, `angles`, `keywords`

### Test Updates
- Updated `ai-local.test.ts` for new extraction behavior (rule-based categories, 3-5 angles, multi-strategy quotes)
- Updated `entryStore.test.ts`, `import.test.ts`, `search.test.ts` with new required Entry fields
- All 106 tests pass

## Key Decisions
- **Wiki-links over backlink panel only:** Bidirectional `[[links]]` are more powerful than just a backlinks sidebar — they create a true knowledge graph
- **Rule-based over random categorization:** Previous implementation used `Math.random()` for categories — replaced with keyword scoring for deterministic, meaningful results
- **Accordion cards over always-visible:** Clickable step bar + expandable sections keeps the UI clean while allowing deep inspection
- **Cluster coloring over monochrome:** Node type coloring makes the graph immediately interpretable
- **Safe defaults everywhere:** Old DB entries lack new fields — every component now uses `|| []` / `|| ''` guards

## Files Changed (23 modified + 1 new)
- `knowflow/src/lib/utils/wikiLinks.ts` — **NEW** — wiki-link extraction, resolution, backlinks, markdown generation
- `knowflow/src/types/entry.ts` — added `linkedEntryIds`, `markdownContent`, `Backlink` type
- `knowflow/src/lib/ai/local.ts` — complete rewrite of all 4 card processors
- `knowflow/src/lib/ai/process.ts` — added wiki-link extraction + bidirectional linking + markdown generation
- `knowflow/src/lib/db/dexie.ts` — added v2 schema migration
- `knowflow/src/stores/entryStore.ts` — added new fields to createEntry
- `knowflow/src/components/entry/EntryDetail.tsx` — complete rewrite: clickable cards, markdown, backlinks
- `knowflow/src/components/entry/EntryCard.tsx` — progress bar, keywords, linked count, safe defaults
- `knowflow/src/components/graph/KnowledgeGraph.tsx` — complete rewrite: cluster layout, interactivity
- `knowflow/src/components/cards/Card2Categorize.tsx` — safe defaults
- `knowflow/src/components/cards/Card3Angles.tsx` — safe defaults
- `knowflow/src/components/cards/Card4Extract.tsx` — safe defaults
- `knowflow/src/components/layout/CaptureModal.tsx` — pass new fields on create
- `knowflow/src/components/layout/Navigation.tsx` — icons, backdrop blur
- `knowflow/src/components/layout/FAB.tsx` — repositioned to avoid nav overlap
- `knowflow/src/app/page.tsx` — smart grouping, stats bar
- `knowflow/src/app/globals.css` — full prose/markdown CSS
- `knowflow/src/lib/utils/export.ts` — safe defaults for goldenQuotes, tags
- `knowflow/tests/unit/ai-local.test.ts` — updated for new extraction behavior
- `knowflow/tests/unit/entryStore.test.ts` — new required fields
- `knowflow/tests/unit/import.test.ts` — new required fields
- `knowflow/tests/unit/search.test.ts` — new required fields

## Current State
- **Builds:** ✅ `npm run build` succeeds
- **Type-check:** ✅ `npx tsc --noEmit` passes
- **Tests:** ✅ 106 pass
- **Local:** ✅ Works on http://localhost:3000
- **Working tree:** Uncommitted (23 modified + 1 new file)
- **Vercel:** Deployed but unreachable from China

## Blockers / Open Questions
- **All changes uncommitted** — need to `git add` and `git commit` before next session
- Wiki-links require users to type `[[Entry Title]]` in raw text — no UI helper yet (could add autocomplete)
- Graph nodes don't show entry titles, only keyword labels — could add entry nodes
- Markdown rendering in cards is basic — could add syntax highlighting for code blocks
- No "Create new entry from wiki-link" flow yet (clicking unresolved `[[link]]` does nothing)

## Next Steps
1. Commit all changes: `git add -A && git commit -m "feat(knowflow): bidirectional links, graph redesign, markdown rendering, null safety"`
2. Test full flow end-to-end: create entry with `[[wiki-links]]` → verify backlinks appear → navigate via links
3. Add wiki-link autocomplete in capture textarea (suggest existing entry titles)
4. Add entry nodes to knowledge graph (not just keyword nodes)
5. Deploy updated version to Vercel
6. Consider adding syntax highlighting for code blocks in markdown

## Context for Next Session
- **Working directory:** `c:\Users\zhong\Desktop\vibe coding\four\knowflow`
- **Repo root:** `c:\Users\zhong\Desktop\vibe coding\four`
- **All changes are uncommitted** — run `git add -A && git commit` first
- **Dev server:** `cd knowflow && npm run dev` — localhost:3000
- **Supabase project:** `tnurpklhndcwfmrwkdns`
- **Remote:** `origin` → `https://github.com/dcctc5kf7z-byte/KnowFlow.git`
- **Dependencies added:** react-markdown, remark-gfm, remark-wiki-link, rehype-raw
- **Dexie schema:** Now at version 2 — auto-migrates old entries with safe defaults
- **Old DB entries:** Will work but won't have `linkedEntryIds`, `markdownContent`, `keywords` etc. until re-processed

---

### Session 7 — 2026-07-05 (Session Transition — No New Work)

## Status
- Session handoff invoked at start — no new code changes
- All prior work (Sessions 1-6) remains uncommitted (23 modified + 1 new file)
- Working tree dirty: run `git add -A && git commit` before starting new work

## Unchanged Priorities
1. Commit Session 6 changes
2. Test full wiki-link flow end-to-end
3. Add wiki-link autocomplete in capture textarea
4. Add entry nodes to knowledge graph
5. Deploy updated version to Vercel
